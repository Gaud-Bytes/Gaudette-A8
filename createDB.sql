DROP DATABASE IF EXISTS USERDB;

CREATE DATABASE USERDB;

use USERDB;

CREATE TABLE USER (
    USER_ID int NOT NULL AUTO_INCREMENT,
    USER_EMAIL varchar(255) UNIQUE NOT NULL,
    USER_PASS varchar(60) NOT NULL,
    USER_LOW_SCORE INT DEFAULT 0,
    PRIMARY KEY (USER_ID)
);

